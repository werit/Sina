apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sina-kafka-ui.fullname" . }}-test-connection"
  labels:
    {{- include "sina-kafka-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sina-kafka-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
