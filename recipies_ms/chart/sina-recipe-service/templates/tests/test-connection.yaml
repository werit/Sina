apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sina-recipe-service.fullname" . }}-test-connection"
  labels:
    {{- include "sina-recipe-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sina-recipe-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
